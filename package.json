{
  "name": "@dcodegroup/vue-mention-monorepo",
  "version": "2.0.1",
  "private": false,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "release": "yarn run lint && lerna publish --dist-tag next"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
